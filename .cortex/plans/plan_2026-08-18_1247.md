---
created: 2026-08-18T12:47:11.361Z
session: 0a60023a-d707-400f-9e4b-85ee273a8e2b
working_directory: c:\Users\MarcelSpindler\Documents\GitHub\rezeptlogik-app
---

## Context

**Untersucht:**
- `src/LinePlanningView.tsx` (2221 Zeilen) — bestehende Drag&Drop Linienplanung
- GSheet "LinePlating W34" Tab — das Ziel-Layout mit Spalten: Day, Comms, Time, P-Line 1/2/3, Cupping/Slicing, Amount, Run, Code, Meal, Planned, Start, Stop, Run Time, Actual, Delta, Comment, Backfills, Plates, Shortage, Labour Planning
- `firebase.json` — fehlender Rewrite für `/api/local-db/gemini-planning-chat`
- `scripts/local-db-server.mjs` — funktionierender Planning-Chat-Endpunkt (nur lokal)
- `functions/index.js` — bestehende Cloud Functions mit GEMINI_API_KEY Secret

**Kernprobleme:**
1. **GSheet-Export-Layout fehlt** — Die bestehende Linienplanung ist eine Drag&Drop-UI, aber kein tabellarisches Copy-Paste-Format wie im GSheet
2. **Cockpit AI kaputt in Production** — `/api/local-db/gemini-planning-chat` hat keinen Firebase-Rewrite, daher kommt `index.html` statt JSON zurück

---

## Implementation Steps

### Teil 1: GSheet-kompatible Plating-Plan-Tabelle (Copy-Paste fähig)

1. **Neue Komponente `src/features/planning-oasis/lines/LinePlatingSheet.tsx`** erstellen — Rendert den Plating-Plan im exakten GSheet-Layout als HTML-Tabelle:
   - Spalten: Day | Comms | Time | P-Line 1 | P-Line 2 | P-Line 3 | Cupping/Slicing | Amount | Run | Code | Meal | Planned | Start | Stop | Run Time | Actual | Delta | Comment | Backfills | Plates | Shortage | Shortage % | Labour Planning
   - Pro Tag: Zeitslots von 06:00–15:00 (30-Min-Intervalle wie im GSheet), "Prep Line", Breaks (08:30–09:00, 11:30–12:00), "Clean Line" (14:45–15:00)
   - Rezeptnamen in den Line-Zellen, mit farblicher Hervorhebung
   - Meals-Gesamtzeile pro Tag mit Summen
   
2. **"Als Tabelle kopieren" Button** in `LinePlanningView.tsx` — Kopiert die Tabelle ins Clipboard als TSV (Tab-Separated), direkt in Google Sheets pastebar

3. **Tab-Switcher** in der Lines-Sektion: "Drag&Drop" (bestehend) | "Sheet-Ansicht" (neu) — letzerer zeigt die GSheet-Tabelle

### Teil 2: Cockpit AI in Production fixen

4. **Cloud Function `geminiPlanningChat`** in `functions/index.js` anlegen — Kopiert die Logik aus `local-db-server.mjs:222-325` (generateGeminiPlanningChat + geminiCallWithRetry) als onRequest Function mit `secrets: [GEMINI_API_KEY_SECRET]`

5. **Firebase Rewrite** in `firebase.json` hinzufügen:
   ```json
   { "source": "/api/local-db/gemini-planning-chat", "function": "geminiPlanningChat", "region": "europe-west3" }
   ```

6. **Deploy Functions + Hosting** — `deploy:all` ausführen

---

## Verification

- Build (`npm run build`) muss fehlerfrei durchlaufen
- Sheet-Ansicht Tab: visuell prüfen dass das Layout dem GSheet "LinePlating W34" entspricht
- Copy-Paste-Test: TSV in Google Sheets einfügen → Spalten müssen korrekt alignen
- Cockpit AI: POST an `/api/local-db/gemini-planning-chat` muss JSON zurückgeben (kein HTML)
- Live deploy: `https://rezeptlogik-verden-factor.web.app/?oase=cockpit` → AI-Chat funktioniert

---

## Critical Files

- `src/LinePlanningView.tsx` — Tab-Switcher + Export-Button hinzufügen
- `src/features/planning-oasis/lines/LinePlatingSheet.tsx` — Neue Komponente (GSheet-Layout)
- `functions/index.js` — Cloud Function `geminiPlanningChat` hinzufügen
- `firebase.json` — Rewrite für planning-chat Endpoint
- `scripts/local-db-server.mjs` — Referenz für die AI-Chat-Logik