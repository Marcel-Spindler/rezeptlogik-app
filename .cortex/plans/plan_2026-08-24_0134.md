---
created: 2026-08-24T01:34:06.983Z
session: eb795893-1f4a-456c-b8a8-2132f541430b
working_directory: c:\Users\MarcelSpindler\Documents\GitHub\rezeptlogik-app
---

## Plan

### 1. Scoop prominenter im PDF anzeigen

**Problem**: Der Scoop wird aktuell nur als kleiner Stat-Tile im 3-Spalten-Grid gerendert (Zeile 340-344 in `ketPdf.ts`). Bei zusammengesetzten WOs steht er als 9px-Text unter dem Component-Header. Er fehlt komplett auf der Rückseite (`card-back`).

**Lösung**: Einen dedizierten, auffälligen Scoop-Block **unterhalb** des Stats-Grids (neben gnTrayHtml/factorBadgeHtml) rendern — mit Farbpunkt, Tool-Name und Portionsgröße. Bei Komponenten-WOs bleibt er je Komponente (dort ebenfalls prominenter). Zusätzlich den Scoop als Info-Zeile auf dem `card-back` wiederholen (damit er auch auf der Rückseite lesbar ist).

**Datei**: `src/features/ket-plan/ketPdf.ts`
- Zeile ~340-344: Den `stat-scoop` Stat-Tile im Grid **entfernen** (zu klein/unauffällig)
- Stattdessen neuen Block nach Zeile ~223 (`gnTrayHtml`) einfügen: eigener `div` mit 🥄-Icon, fetter Darstellung, ca. gleicher Stil wie gnTrayHtml
- Zeile ~371 (`card-back`): Nach `back-id` eine Scoop-Zeile einfügen (wenn vorhanden)
- Bei Komponenten (Zeile 142-143): Styling etwas größer/auffälliger machen (font-size 10px statt 9px)

### 2. Dateiname: WO + KW zuerst

**Problem**: Aktuell `WO_<nummer>_<recipeCode>_<subRecipeName>` — user will WO-Nummer und KW zuerst.

**Lösung**: `woFilename()` (Zeile 94-99 in `KetBreakdownView.tsx`) ändern zu Format:
`WO_<nummer>_KW<xx>_<recipeCode>_<subRecipeName>`

KW extrahieren aus der WO-Nummer (die ersten 2 Ziffern vor dem Bindestrich, z.B. "36-68" → KW36) — dafür existiert bereits `weekPrefixFromWoNumber()` im Projekt.

**Datei**: `src/KetBreakdownView.tsx`
- Zeile 94-99: `woFilename` anpassen, KW aus `row.woNumber` extrahieren und vorne einbauen

### Verification
- App starten, KET-Plan mit Daten laden
- Einzelne WO als PDF speichern → Dateiname prüft `WO_36-68_KW36_...`
- PDF öffnen → Scoop-Info als eigener Block sichtbar auf Vorder- UND Rückseite
- Drucken → Scoop auf dem Ausdruck gut lesbar

### Critical Files
- `src/features/ket-plan/ketPdf.ts` — PDF-HTML-Generierung, Scoop-Darstellung
- `src/KetBreakdownView.tsx` — `woFilename()` Funktion für Dateinamen