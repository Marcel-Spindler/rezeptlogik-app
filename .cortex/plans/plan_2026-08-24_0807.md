---
created: 2026-08-24T08:07:50.127Z
session: 92dc46a4-b312-417a-9174-92fb6a0e56b9
working_directory: c:\Users\MarcelSpindler\Documents\GitHub\rezeptlogik-app
---

## Context

Die `PlhReadyToPlateBoard` in `WmsMealOperations.tsx` zeigt aktuell:
- "bereit" Meals nur als Text-Badge "✓ 5/5 im PLH"
- "fast bereit" Meals als einzeilige Textliste "fehlt: X, Y, Z"

## Plan

Ersetze die bestehende Darstellung durch eine grafische Submeal-Visualisierung pro Meal:

1. **Segmentierte Fortschrittsleiste** pro Meal — jedes Segment = 1 Submeal (grün = vorhanden, rot/leer = fehlt)
2. **Submeal-Grid** darunter: kleine Chips/Badges für jedes Submeal mit Status-Farbe (grün = im PLH, rot = fehlt), Name und Menge
3. Für "fast bereit" Meals: die fehlenden Submeals prominent in rot hervorgehoben mit Name

Änderungen nur in einer Datei: `src/features/wms-overview/WmsMealOperations.tsx` — die `PlhReadyToPlateBoard` Komponente wird erweitert.

## Verification
- `npx tsc --noEmit` 
- Visuell im Browser prüfen

## Critical Files
- `src/features/wms-overview/WmsMealOperations.tsx` — Einzige Datei die geändert wird