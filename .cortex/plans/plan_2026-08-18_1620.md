---
created: 2026-08-18T16:20:27.914Z
session: af47cd09-85b9-44f9-9b03-6b1db83d58f1
working_directory: c:\Users\MarcelSpindler\Documents\GitHub\rezeptlogik-app
---

## Plan: KET Plan / WO — Alle 13 Verbesserungen

### Dateien die geändert werden:
- `src/features/ket-plan/ketLogic.ts` (Hauptlogik)
- `src/features/ket-plan/factorRules.ts` (Klassifizierung)
- `src/features/ket-plan/ketTypes.ts` (neue Felder)
- `src/__tests__/ketLogic.test.ts` (Tests erweitern)

---

### Bug-Fixes (1–5)

**#1 — UoM-Fallback "assume grams" korrigieren** (`ketLogic.ts:304-309`)
- Explizite Erkennung von `ml`/`l` (→ 1:1 Näherung zu kg), `pcs`/`stk`/`ea` (→ 0, separat als Stückzahl)
- Warnung im Ergebnis wenn unbekannte UoM vorkommt (neues Feld `uomWarnings: string[]` in `BatchCalc`)

**#2 — Doppelter `matchGrossIngredients`-Aufruf eliminieren** (`ketLogic.ts:329+360`)
- Ergebnis einmal berechnen und in Variable cachen, zweiten Aufruf entfernen

**#3 — `parseSortKey` robuster bei ungültigem Datum** (`ketLogic.ts:173-176`)
- Fallback auf `0` wenn `Date.parse` NaN zurückgibt

**#4 — Sauce-vs-Fleisch-Priorität in `factorRules.ts`** (Zeile 74-77)
- `/sauce/i`-Check vor den Fleisch-Regeln einfügen (nicht nur bei führendem "Sauce"), damit "Chicken Teriyaki Sauce" als Sauce klassifiziert wird

**#5 — Batch-Berechnung vereinfachen** (`ketLogic.ts:434-441`)
- Klarere Logik: `batches = totalKg > 0 ? Math.ceil(totalKg / cap) : 0`

---

### Robustheit (6–8)

**#6 — CSV-Parse-Fehler melden** (`ketLogic.ts:parseKetCsv`)
- `result.errors` auswerten, Rückgabetyp erweitern zu `{ rows: KetRow[]; warnings: string[] }`
- Bestehende Aufrufer anpassen (KetBreakdownView)

**#7 — Instruction-Priorität dokumentieren** (`ketLogic.ts:findSubRecipeInstructions`)
- Kommentar mit Prioritätsreihenfolge + Konstante für Reihenfolge

**#8 — Normalisierung vereinheitlichen** 
- `normStr` und `normBibleStr` bleiben getrennt (verschiedene Anwendungsfälle), aber klarer dokumentieren wann welche zu verwenden ist

---

### Erweiterungen (9–13)

**#9 — Utilization-Metrik** (neues Feld `utilizationPct` in `EquipBatch`)
- `remainderKg / capacityKg * 100` — wie voll ist der letzte Batch

**#10 — UoM-Konvertierung erweitern**
- `ml`→kg (Faktor 0.001), `l`→kg (Faktor 1), `pcs`/`stk`/`ea` als Stückzahl-Feld separat
- Neues Feld `totalPcs: number` in `IngCalc` für Stück-Zutaten

**#11 — Bible-Match Confidence** (nicht jetzt — zu invasiv, Substringlänge reicht aktuell)
- Stattdessen: `matchQuality: "exact" | "substring" | "none"` im `EquipBatch.bibleMatch`

**#12 — Factor vs. equipBatches Widerspruch auflösen**
- Neues Feld `factorOverridesEquip: boolean` in `BatchCalc` — wenn Factor `NO_BATCH` sagt, wird in der UI klar angezeigt dass Equipment-Batches ignoriert werden sollen

**#13 — Tests erweitern**
- Neuer Test: UoM-Handling (ml, l, pcs)
- Neuer Test: "Chicken Teriyaki Sauce" → Sauce (nicht NO_BATCH)
- Neuer Test: CSV mit Parse-Fehlern → warnings
- Neuer Test: parseSortKey mit ungültigem Datum

---

### Nicht gemacht (bewusst ausgeklammert):
- #9 "Mehrstufen-Equipment-Planung" (sequentielle Steps) — zu großer Scope, eigenes Feature
- #12 "Jaccard-Score" — Overkill, Substringlänge funktioniert